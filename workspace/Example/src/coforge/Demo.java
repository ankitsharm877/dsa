package coforge;

import java.util.stream.Collectors;

public class Demo {

	public static void main(String[] args) {
		//employee (id, name, salary, city, dept)
		// avg salary in each dept
		
		//list.stream().collect(Collectors.groupingBy(Employee::getDept), 
		//Collectors.averagingInt(Employee::getSalary), Collectors.toList())
	}
}

